@model TasksHeaderViewModel

<div class="flex items-baseline justify-between gap-2 p-3 border-b">
    <h3 class="text-3xl font-thin">@Model.Title</h3>
    <div class="flex flex-col">
    <p class="text-xs text-gray-700">Created by: @Model.Creator</p>

    </div>
</div>