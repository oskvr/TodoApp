@model IEnumerable<SidebarListItemViewModel>

<div class="mt-10" id="sticky-lists">
    <ul>
        @foreach (var viewModel in Model)
        {
            <partial name="_SidebarListItemPartial" model="viewModel" />
        }
    </ul>
</div>